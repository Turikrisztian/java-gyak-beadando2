package com.example.beadando.repository;

import com.example.beadando.entity.Targy;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TargyRepository extends JpaRepository<Targy, Long> {
}
